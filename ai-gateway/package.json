{
  "name": "ai-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "AI provider gateway (OpenAI, Claude, Ollama)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
